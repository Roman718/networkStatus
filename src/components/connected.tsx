import React from 'react';

const Connected = () => {
    return (
        <div className='status connected'>
            connected <span className='flag'> </span>
        </div>
    );
};

export default Connected;